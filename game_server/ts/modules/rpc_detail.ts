export enum Err {
    Login = "login",
    RepeatLog = "repeatLog",
}

export enum Message {
    Waiting = "waiting",
    Bye = "bye",
}

export enum FightLog {
    AlreadyKill = "alreadyKill",
    AlreadyDied = "alreadyDied",
    Attacking = "attacking",
    AttackOver = "attackOver",
    KillSteal = "killSteal",
}

export enum Request {
    WaitMonster = "waitMonster",
}
